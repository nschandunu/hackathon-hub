services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    image: hackathon-hub-web
    container_name: hackathon-hub-app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    command: pnpm run dev
    restart: unless-stopped